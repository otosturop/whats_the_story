import 'package:flutter/rendering.dart';

class PagePadding extends EdgeInsets {
  const PagePadding.allNormal() : super.all(30);
  const PagePadding.allLow() : super.all(15);
  const PagePadding.allToLow() : super.all(10);
}
